name: 🚀 FTP Deploy

on:
  push:
    branches:
      - main  # main ব্রাঞ্চে push করলে trigger হবে

jobs:
  ftp-deploy:
    name: 📤 Upload via FTP
    runs-on: ubuntu-latest

    steps:
      - name: 🔄 Checkout code
        uses: actions/checkout@v4

      - name: 📁 Upload to FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.4.0
        with:
          server: ftpupload.net
          username: b33_39554238
          password: nokhil22
          protocol: ftp
          port: 21
          server-dir: /htdocs/  # এখানে আপনি আপনার FTP ফোল্ডার দিতে পারেন
          local-dir: ./              # আপনার প্রজেক্টের কোন ফোল্ডার আপলোড হবে